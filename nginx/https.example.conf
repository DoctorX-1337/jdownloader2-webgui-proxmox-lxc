# With a trusted certificate: replace port 80 or use a separate server block.
# listen 443 ssl;
# ssl_certificate /etc/jdownloader-web/tls/server.crt;
# ssl_certificate_key /etc/jdownloader-web/tls/server.key;
# ssl_protocols TLSv1.2 TLSv1.3;
# Also set COOKIE_SECURE=true in config.env and restart jdownloader-web.
